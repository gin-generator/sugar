package providers

// Service names for dependency injection container
const (
	ServiceLogger  = "logger"
	ServiceDB      = "db"
	ServiceCache   = "cache"
	ServiceStorage = "storage"
	ServiceQueue   = "queue"
)
